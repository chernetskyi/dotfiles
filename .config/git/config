[user]
  name = Volodymyr Chernetskyi
  email = 19735328+chernetskyi@users.noreply.github.com
  signingkey = F0E3704751532A30
  useConfigOnly = true

[core]
  editor = helix

[commit]
  gpgsign = true

[init]
  defaultBranch = main

[merge]
  autoStash = true

[push]
  autoSetupRemote = true

[rebase]
  autoStash = true
  autoSquash = true

; alias {{{
[alias]
  a    = add
  af   = add --force
  b    = branch
  bl   = blame
  bs   = bisect
  c    = commit --allow-empty
  ca   = commit --amend --no-edit
  ce   = commit --allow-empty --amend
  cf   = config
  ch   = checkout
  cl   = clone --no-progress
  cp   = cherry-pick
  d    = diff
  dd   = diff --staged
  dl   = clone --no-progress --depth 1 --single-branch --branch
  ds   = diff --compact-summary
  dt   = difftool
  f    = fetch
  fu   = commit --fixup
  i    = init
  l    = log -n 10 --decorate --graph --format='%Cred%h %Cblue%cn %Cgreen%cr %Creset%s'
  ll   = log --decorate --graph --format='%Cred%h %Cblue%cn %Cgreen%cr %Creset%s'
  m    = merge
  ms   = merge --squash
  nb   = checkout -B
  pl   = pull --ff-only
  plf  = pull --force
  pop  = stash pop
  ps   = push --no-progress
  psf  = push --no-progress --force-with-lease --force-if-includes
  r    = rebase
  rank = shortlog -nse --no-merges
  rh   = reset --hard
  ri   = rebase --interactive
  rl   = reflog
  rm   = rm -r
  rmt  = remote
  root = rev-parse --show-toplevel
  rs   = reset --soft
  rv   = revert --no-edit
  rvh  = revert --no-edit HEAD
  s    = status --short
  sb   = clone --no-progress --single-branch --branch
  sh   = show
  sl   = shortlog
  st   = stash
  sw   = switch
  t    = tag
  tf   = tag --force
; }}}

[credential "https://github.com"]
  helper = 
  helper = !/usr/bin/gh auth git-credential

[credential "https://gist.github.com"]
  helper = 
  helper = !/usr/bin/gh auth git-credential

; vim:foldmethod=marker:foldlevel=0
