[user]
    name = Volodymyr Chernetskyi
    email = 19735328+chernetskyi@users.noreply.github.com
    signingkey = F0E3704751532A30
    useConfigOnly = true

[core]
    editor = nvim

[commit]
    gpgsign = true

[push]
    autoSetupRemote = true

[init]
    defaultBranch = main

; alias {{{
[alias]
    a    = add --force
    b    = branch
    c    = commit --allow-empty
    d    = diff
    f    = fetch
    i    = init
    l    = log --decorate --graph --format='%Cred%h %Cblue%cn %Cgreen%cr %Creset%s'
    m    = merge
    r    = rebase
    s    = status --short
    t    = tag
    bl   = blame
    bs   = bisect
    ca   = commit --amend --no-edit
    ce   = commit --allow-empty --amend
    cf   = config
    ch   = checkout
    cl   = clone
    cp   = cherry-pick
    dd   = diff --staged
    dl   = clone --depth 1 --single-branch --branch
    ms   = merge --squash
    nb   = checkout -B
    pl   = pull --ff-only
    ps   = push
    rh   = reset --hard
    ri   = rebase --interactive
    rl   = reflog
    rm   = rm -r
    rs   = reset --soft
    rv   = revert
    sb   = clone --single-branch --branch
    sh   = show
    sl   = shortlog
    st   = stash
    sw   = switch
    tf   = tag --force
    plf  = pull --force
    pop  = stash pop
    psf  = push --force
    rmt  = remote
    rank = shortlog -nse --no-merges
    root = rev-parse --show-toplevel
; }}}

[credential "https://github.com"]
  helper = 
  helper = !/usr/bin/gh auth git-credential

[credential "https://gist.github.com"]
  helper = 
  helper = !/usr/bin/gh auth git-credential

; vim:foldmethod=marker:foldlevel=0
